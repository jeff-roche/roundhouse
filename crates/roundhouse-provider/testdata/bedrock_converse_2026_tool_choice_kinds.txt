# The full `ToolChoice` union, fetched verbatim from
# https://docs.aws.amazon.com/bedrock/latest/APIReference/API_runtime_ToolChoice.html
# (fetched 2026-09-02): exactly 3 members. There is deliberately no member
# meaning "the model must not call any tool" -- see this codec's
# `encode.rs`'s `EncodeError::ToolChoiceNoneUnsupported` doc comment for the
# verified divergence this enables `encode.rs` to fail closed on.
any
auto
tool

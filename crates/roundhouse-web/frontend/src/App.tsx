// Placeholder shell. Task 10 slice B builds `RunsInbox.tsx` and
// `SessionView.tsx` against `./api`'s client surface; this slice's job is
// everything underneath them.
export function App() {
  return <p>Roundhouse web UI is booting.</p>;
}
